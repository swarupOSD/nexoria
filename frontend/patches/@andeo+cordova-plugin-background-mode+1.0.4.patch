diff --git a/node_modules/@andeo/cordova-plugin-background-mode/plugin.xml b/node_modules/@andeo/cordova-plugin-background-mode/plugin.xml
index 1169497..c70d294 100644
--- a/node_modules/@andeo/cordova-plugin-background-mode/plugin.xml
+++ b/node_modules/@andeo/cordova-plugin-background-mode/plugin.xml
@@ -71,11 +71,14 @@
         </config-file>
 
         <config-file target="AndroidManifest.xml" parent="/manifest/application">
-            <service android:name="de.appplant.cordova.plugin.background.ForegroundService" />
+            <service android:name="de.appplant.cordova.plugin.background.ForegroundService" android:foregroundServiceType="specialUse">
+                <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" android:value="Keep webview alive for background media" />
+            </service>
         </config-file>
 
         <config-file target="AndroidManifest.xml" parent="/manifest">
             <uses-permission android:name="android.permission.WAKE_LOCK" />
+            <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
         </config-file>
 
         <source-file
diff --git a/node_modules/@andeo/cordova-plugin-background-mode/src/android/ForegroundService.java b/node_modules/@andeo/cordova-plugin-background-mode/src/android/ForegroundService.java
index 8a89184..028dcce 100644
--- a/node_modules/@andeo/cordova-plugin-background-mode/src/android/ForegroundService.java
+++ b/node_modules/@andeo/cordova-plugin-background-mode/src/android/ForegroundService.java
@@ -129,7 +129,12 @@ public class ForegroundService extends Service {
         boolean isSilent    = settings.optBoolean("silent", false);
 
         if (!isSilent) {
-            startForeground(NOTIFICATION_ID, makeNotification());
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
+                // Use SPECIAL_USE (1073741824) to bypass Android 14's strict MediaSession requirements for mediaPlayback type
+                startForeground(NOTIFICATION_ID, makeNotification(), 1073741824);
+            } else {
+                startForeground(NOTIFICATION_ID, makeNotification());
+            }
         }
 
         PowerManager pm = (PowerManager)getSystemService(POWER_SERVICE);
