name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        run: curl -X POST "https://api.render.com/deploy/srv-d9kd6h5bedkc739hrcfg?key=W3CfzpkA38o"
